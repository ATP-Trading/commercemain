# Historical custom-membership documentation

These documents are archived unchanged for reference. They are NOT current operational procedures and must not be used to deploy, configure, migrate, authenticate or manage the live store. Their custom admin, scheduler and membership setup instructions refer to retired implementations.

Use [the current storefront release checklist](../STOREFRONT_RELEASE_CHECKLIST.md). Do not re-enable the former browser-only admin gate or run the old data/Shopify setup instructions. This archive does not indicate that any merchant-side settings were modified.
